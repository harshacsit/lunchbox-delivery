package com.lunchbox.delivery.activities;

import android.os.Bundle;
import androidx.appcompat.app.AppCompatActivity;

/**
 * @deprecated This activity is no longer used. Use MainActivity instead.
 * This file is kept as an empty shell to avoid compilation errors from stale references.
 */
public class DeliveryListActivity extends AppCompatActivity {
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        finish();
    }
}